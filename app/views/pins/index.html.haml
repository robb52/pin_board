#pins.transitions-enabled
  - @pins.each do |pin|
    .box.card.card-default
      = link_to (image_tag pin.image.url), pin
      .card-body
        %h2= link_to pin.title, pin
        %p.user
        Submitted by:
        = pin.user.email